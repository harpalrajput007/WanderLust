<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WanderLust</title>
</head>
<body>
    <h3>Edit your listing</h3>
    <form method="POST" action="/listings/<%= oneListing._id %>?_method=PUT">
        <input type="text" name="listing[title]" value="<%= oneListing.title %>" >
        <br>
        <br>
        <textarea name="listing[description]" id=""><%= oneListing.description %></textarea>
        <br>
        <br>
        <input type="text" name="listing[image]" value="<%= oneListing.image.url %>" >
        <br>
        <br>
        <input type="number" name="listing[price]" value="<%= oneListing.price %>" >
        <br>
        <br>
        <input type="text" name="listing[location]" value="<%= oneListing.location %>" >
        <br>
        <br>
        <input type="text" name="listing[country]" value="<%= oneListing.country %>" >
        <br>
        <br>
        <button>Edit</button>
    </form>
</body>
</html>